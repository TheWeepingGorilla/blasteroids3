var classes

(c => {
 	"use strict"

 	class Collision {
 		constructor(sketch, objects) {
 			this.p = sketch
 			this.objectsX = objects
 			this.objectsY = objects
 		}
 		collisionsCheck(objects) {

 		}
 		collisionCheck(object1, object2) {

 		}
 		objectsSort() {

 		}
 	}

 	c.Collision = Collision
})(classes || (classes = {}))